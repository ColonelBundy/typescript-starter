{
  "name": "typescript-starter",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/*",
    "compile": "yarn clean && tsc && node dist/index.js",
    "dev": "nodemon -e ts --exec \"yarn compile\"",
    "debug": "yarn clean && tsc && node --inspect dist/index.js"
  },
  "engines": {
    "node": ">=8.9.1"
  },
  "author": "Colonelbundy <colonelbundy@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.2"
  },
  "devDependencies": {
    "@types/express": "^4.0.35",
    "@types/node": "^9.3.0",
    "nodemon": "^1.11.0",
    "rimraf": "^2.6.1",
    "typescript": "^2.6.2"
  }
}
